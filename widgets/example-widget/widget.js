// Trust Badge Bar — no initialization needed, pure CSS widget.
// This file is intentionally minimal.
(function () {
  var bar = document.querySelector('.trust-badge-bar');
  if (bar) {
    bar.setAttribute('data-widget', 'trust-badge-bar');
  }
})();
