# Dependencies
node_modules

# Secrets / environment (never commit real keys — use *.example instead)
.env
server/.env

# OS junk
.DS_Store

# Local editor / agent settings
.claude/settings.local.json

# Client build output (generated by `npm run build`)
client/dist

# Runtime session data (temporary offer working dirs)
server/sessions
